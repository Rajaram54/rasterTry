export interface IProduct {
    ProductID: number;
    ProductName: string;
 }
//  if(result && result['result']){
//     for (let x = 0; x < result['result'].length; x++) {
      
//       let id=result['result'][x].id;
//       let name=result['result'][x].name;
//       let email=result['result'][x].email;
//       let password=result['result'][x].password;
//       let designation=result['result'][x].designation;
//       let createdAt=result['result'][x].createdAt;
//       let updatedAt=result['result'][x].updatedAt;


//       this.obj.push({ id, name,email, password,designation,createdAt,updatedAt });